name = "chumi-cron-worker"
main = "index.js"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[triggers]
crons = [
  "*/30 * * * *",   # частые задачи (streaks, cleanup, summary в 9 UTC)
  "0 18 * * *"      # ежедневное напоминание — раз в сутки в 18:00 UTC
]

