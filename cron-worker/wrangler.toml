name = "chumi-cron-worker"
main = "index.js"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

[triggers]
crons = [
  "*/30 * * * *"   # все задачи: streaks/cleanup каждые 30 мин; напоминания в 18:00 UTC и сводка в 9:00 UTC — один раз в день (логика внутри index.js)
]
